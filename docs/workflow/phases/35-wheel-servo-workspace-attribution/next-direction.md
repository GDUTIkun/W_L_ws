# Phase 35 next direction

Exactly one next experiment is selected: an offline wheel-spin/mesh-phase validity audit at fixed
base, leg geometry and contact state. Sweep the canonical wheel angle through and beyond `±1 rad`,
preserve the current rotating mesh and runtime gate, and compare closure, contact geometry,
mass/bias/Jacobians and dynamically equivalent phase-separated samples. Do not bypass the gate in
closed loop.

This is the minimum experiment that can distinguish a necessary rotating-mesh validity interval from
`P35-I_workspace_contract_or_validity_gate_issue`. Phase35 does not pre-classify P35-I and implements
no repair.
