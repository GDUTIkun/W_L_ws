# REJECTED

The evaluator failed before producing evidence because its geometry record did not
carry the wheel axis. The evaluator now reconstructs that axis from the frozen
MuJoCo body pose. Superseded by the next append-only formal directory.
